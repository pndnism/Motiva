<%= form_with(model: habit, local: true) do |form| %>
  <% if habit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(habit.errors.count, "error") %> prohibited this habit from being saved:</h2>

      <ul>
        <% habit.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :personal_habit_id %>
    <%= form.text_field :personal_habit_id %>
  </div>

  <div class="field">
    <%= form.label :habit %>
    <%= form.text_field :habit %>
  </div>

  <div class="field">
    <%= form.label :habituation %>
    <%= form.number_field :habituation %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
