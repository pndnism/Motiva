<%= form_with(model: habit_history, local: true) do |form| %>
  <% if habit_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(habit_history.errors.count, "error") %> prohibited this habit_history from being saved:</h2>

      <ul>
        <% habit_history.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
